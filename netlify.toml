[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# Serve custom 404 page for unknown paths
[[redirects]]
  from   = "/*"
  to     = "/404.html"
  status = 404
